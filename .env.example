# ======================================
# Supabase Configuration
# ======================================
# URL pública de tu proyecto Supabase (requerido)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here

# Clave anónima pública (requerido)
# IMPORTANTE: Esta clave es pública y se expone en el cliente
# Solo debe tener permisos limitados en Supabase
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Clave de servicio de Supabase (solo para uso en servidor)
# IMPORTANTE: NUNCA exponer esta clave en el cliente
# NUNCA usar como variable NEXT_PUBLIC_*
SUPABASE_SERVICE_KEY=your_service_key_here

# ======================================
# Configuración de Seguridad
# ======================================
# Secreto para firmar cookies y tokens (min. 32 caracteres)
# Usar: openssl rand -base64 32
SECRET_COOKIE_PASSWORD=your_cookie_secret_here

# Tiempo de expiración de sesión en segundos (default: 12 horas)
SESSION_EXPIRY_SECONDS=43200

# Dominios permitidos para OAuth (separados por comas)
ALLOWED_AUTH_DOMAINS=innovare.lat,gmail.com

# Límites de tasa para APIs (solicitudes por minuto)
API_RATE_LIMIT=30

# ======================================
# Configuración de Logging
# ======================================
# Nivel de log (debug, info, warn, error)
LOG_LEVEL=info

# Habilitar logging en base de datos
ENABLE_DB_LOGGING=true

# Webhook para alertas de seguridad (opcional)
# SECURITY_ALERT_WEBHOOK=https://hooks.slack.com/services/XXX/YYY/ZZZ

# ======================================
# Configuración de Entorno
# ======================================
# Entorno de ejecución (development, production, test)
NODE_ENV=development

# URL base de la aplicación
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ======================================
# Configuración de GitHub (opcional)
# ======================================
# Token de acceso personal de GitHub
# GITHUB_PERSONAL_ACCESS_TOKEN=your_github_pat_here
